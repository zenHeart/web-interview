import mergeArrays from '!!raw-loader!./answers/array/mergeArrays.js';
import mergeArraysTest from '!!raw-loader!./answers/array/mergeArrays.test.js';
import threeSum from '!!raw-loader!./answers/array/threeSum.js';
import threeSumTest from '!!raw-loader!./answers/array/threeSum.test.js';
import twoSum from '!!raw-loader!./answers/array/twoSum.js';
import twoSumTest from '!!raw-loader!./answers/array/twoSum.test.js';

# 数组

本章准确的说不算算法内容，只是针对数组常用操作做一些说明，

## 说一下 Array 的常用方法? {#p0-array-method}

<Answer>

**延伸阅读**

* [MDN Array](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Array)
* [The Array Type](https://book.douban.com/subject/37143075/) 第六章

</Answer>

## 数组合并？{#p0-arr-merge}

给定两个有序数组 s1、s2, 将 s1 和 s2 合并成一个有序数组。注意 s1 的长度可以容纳 s2 的所有元素。

```js
const s1 = [1, 3, 5, 6, undefined, undefined, undefined];
const s2 = [3, 10];

function mergeArrays(arr1, arr2) {

}
```

<Answer>

<TestCode
   files={{
      "/mergeArrays.js": mergeArrays,
      "/mergeArrays.test.js": mergeArraysTest,
   }}
/>

</Answer>

## 三数求和？{#p0-3arr-sum}

给定数组 s1, 返回所有 array[i, j, k] 的和为 0 的解，对应索引组成的集合

```js
const s1 = [-1, -2, 1, 2, 0];

function threeSum(arr) {

}
```

<Answer>

<TestCode
   files={{
      "/threeSum.js": threeSum,
      "/threeSum.test.js": threeSumTest,
   }}
/>

</Answer>

## 两数求和问题？{#p0-two-sum}

给定一个数组 S 和一个整数 target，在数组 S 中找到两个数，使得它们的和等于 target。例如

```js
const s1 = [1, 3, 5, 6];
const target = 9;

function twoSum(arr, target) {

}
```

<Answer>

<TestCode
   files={{
      "/twoSum.js": twoSum,
      "/twoSum.test.js": twoSumTest,
   }}
/>

</Answer>
