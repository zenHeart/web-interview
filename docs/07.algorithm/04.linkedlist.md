
import arrayToList from '!!raw-loader!./answers/linkedlist/arrayToList.js';

# 链表

链表设计的核心知识点包括

1. 基本结构
2. 哨兵节点使用
3. 快慢指针
4. 多指针操作

## 数组转换为链表? {#p0-array-to-linkedlist}

```js
// 实现 arrayToList 函数，将数组转换为链表
// 输入：[1, 2, 3, 4, 5]
// 输出：{ value: 1, next: { value: 2, next: { value: 3, next: { value: 4, next: { value: 5, next: null } } } } }
function arrayToList (arr) {

}
```

<Answer>

import arrayToListTest from '!!raw-loader!./answers/linkedlist/arrayToList.test.js';

<TestCode
   files={{
      "/arrayToList.js": arrayToList,
      "/arrayToList.test.js": arrayToListTest,
   }}
/>

</Answer>

## 单向链表结构实现? {#p0-single-linkedlist}

```ts
// 实现链表类，支持如下核心方法
// 节点
interface Node {
  value: number
  next: Node | null
}
// 链表
abstract class LinkedList {
  constructor(values: number[]): LinkedList // 构造函数，接收一个数组作为参数，将数组转换为链表
  add(value: any):void // 往链表末尾添加节点
  delete(value: any):void // 删除值为 value 的节点
  deleteValues(values: any[]):void // 批量删除，删除值在 values 数组中的节点
  find(value: any):Node | null // 查找值为 value 的节点
  head:Node | null // 获取链表头节点
  tail:Node | null // 获取链表尾节点
}
```

<Answer>

import LinkedList from '!!raw-loader!./answers/linkedlist/LinkedList.js';
import LinkedListTest from '!!raw-loader!./answers/linkedlist/LinkedList.test.js';

<TestCode
   files={{
      "/LinkedList.js": LinkedList,
      "/LinkedListTest.test.js": LinkedListTest,
   }}
/>

</Answer>

## 合并两个有序链表 {#p0-merge-linkedlist}

```js
/**
* 合并两个有序链表
* 输入：l1 = [1,2,4], l2 = [1,3,4]
* 输出：[1,1,2,3,4,4]
 */

function mergeLinkedList (l1, l2) {

}
```

<Answer>

import mergeLinkedList from '!!raw-loader!./answers/linkedlist/mergeLinkedList.js';
import mergeLinkedListTest from '!!raw-loader!./answers/linkedlist/mergeLinkedList.test.js';

<TestCode
   files={{
      "/mergeLinkedlist.js": mergeLinkedList,
      "/arrayToList.js": arrayToList,
      "/mergeLinkedlist.test.js": mergeLinkedListTest,
   }}
/>

</Answer>

## 删除存在重复值的节点 {#p0-delete-duplicate}

```js
/**
 * 删除链表中重复的元素
 * 输入：[1, 1, 2, 3, 3]
 * 输出：[2, 3]
 */
function removeDuplicate (head) {
}
```

<Answer>

import removeDuplicate from '!!raw-loader!./answers/linkedlist/removeDuplicate.js';
import removeDuplicateTest from '!!raw-loader!./answers/linkedlist/removeDuplicate.test.js';

<TestCode
   files={{
      "/removeDuplicate.js": removeDuplicate,
      "/arrayToList.js": arrayToList,
      "/removeDuplicate.test.js": removeDuplicateTest,
   }}
/>

</Answer>

## 删除倒数第 n 个节点 {#p0-delete-nth}

```js
/**
 * 删除链表的倒数第 n 个节点
 * 输入：[1, 2, 3, 4, 5], n = 2
 * 输出：[1, 2, 3, 5]
 */
function removeNthFromEnd (head, n) {
}
```

<Answer>

import removeNthFromEnd from '!!raw-loader!./answers/linkedlist/removeNthFromEnd.js';
import removeNthFromEndTest from '!!raw-loader!./answers/linkedlist/removeNthFromEnd.test.js';

<TestCode
   files={{
      "/removeNthFromEnd.js": removeNthFromEnd,
      "/removeNthFromEnd.test.js": removeNthFromEndTest,
      "/arrayToList.js": arrayToList,
   }}
/>

</Answer>

## 翻转链表 {#p0-reverse-linkedlist}

```js
// 反转链表 时间复杂度 O(n)，空间复杂度 O(1)
// 输入：[1, 2, 3, 4]
// 输出：[4, 3, 2, 1]
function reverseList (head) {
}
```

<Answer>

import reverseList from '!!raw-loader!./answers/linkedlist/reverseList.js';
import reverseListTest from '!!raw-loader!./answers/linkedlist/reverseList.test.js';

<TestCode
   files={{
      "/reverseList.js": reverseList,
      "/reverseList.test.js": reverseListTest,
      "/arrayToList.js": arrayToList,
   }}
/>

</Answer>

## k 接翻转链表 {#p0-k-reverse-linkedlist}

<!-- TOOD: 补充此问题答案 -->

```js
/**
 * K 个一组翻转链表
 * 输入：[1, 2, 3, 4, 5], k = 2
 * 输出：[2, 1, 4, 3, 5]
 */
function reverseKGroup (head, k) {
}
```

## 判断链表是否有环 {#p0-cycle-check}

```js
/**
 * 判断是否有环
 */
function cycleCheck (head) {
}
```

<Answer>

import cycleCheck from '!!raw-loader!./answers/linkedlist/cycleCheck.js';
import cycleCheckTest from '!!raw-loader!./answers/linkedlist/cycleCheck.test.js';
import fixture from '!!raw-loader!./answers/linkedlist/fixture/cycle.js';

<TestCode
   files={{
      "/cycleCheck.js": cycleCheck,
      "/cycleCheck.test.js": cycleCheckTest,
      "/fixture/cycle.js": fixture,
   }}
/>

</Answer>

## 定位环起点 {#p0-cycle-start}
