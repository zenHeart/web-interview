import testData from '!!raw-loader!./answers/sort/fixture/testData.js';

# 排序✅

## 常见数组排序算法有哪些？ {#p0-sort}

![01_10](https://user-images.githubusercontent.com/22188674/222961047-79023840-bd63-4a2c-93fe-2d94f2a8ac04.png)

## 冒泡排序

<Answer>

比较相邻的元素。如果第一个比第二个大，就交换他们两个。
对每一对相邻元素作同样的工作，从开始第一对到结尾的最后一对。这步做完后，最后的元素会是最大的数。
针对所有的元素重复以上的步骤，除了最后一个。
持续每次对越来越少的元素重复上面的步骤，直到没有任何一对数字需要比较。

* [冒泡 wiki](https://zh.wikipedia.org/wiki/%E5%86%92%E6%B3%A1%E6%8E%92%E5%BA%8F)

import bubbleSort from '!!raw-loader!./answers/sort/bubbleSort.js';
import bubbleSortTest from '!!raw-loader!./answers/sort/bubbleSort.test.js';

<TestCode
   files={{
      "/bubbleSort.js": bubbleSort,
      "/bubbleSort.test.js": bubbleSortTest,
      "/fixture/testData.js": testData,
   }}
/>

</Answer>

## 快速排序

<Answer>

先从数列中取出一个数作为“基准”。

分区过程：将比这个“基准”大的数全放到“基准”的右边，小于或等于“基准”的数全放到“基准”的左边。
再对左右区间重复第二步，直到各区间只有一个数。

import quickSort from '!!raw-loader!./answers/sort/quickSort.js';
import quickSortTest from '!!raw-loader!./answers/sort/quickSort.test.js';

<TestCode
   files={{
      "/quickSort.js": quickSort,
      "/quickSort.test.js": quickSortTest,
      "/fixture/testData.js": testData,
   }}
/>

</Answer>

## 选择排序

<Answer>

首先在未排序序列中找到最小（大）元素，存放到排序序列的起始位置
再从剩余未排序元素中继续寻找最小（大）元素，然后放到已排序序列的末尾。
重复第二步，直到所有元素均排序完毕。

import selectionSort from '!!raw-loader!./answers/sort/selectionSort.js';
import selectionSortTest from '!!raw-loader!./answers/sort/selectionSort.test.js';

<TestCode
   files={{
      "/selectionSort.js": selectionSort,
      "/selectionSort.test.js": selectionSortTest,
      "/fixture/testData.js": testData,
   }}
/>

</Answer>

## 插入排序

<Answer>

将第一待排序序列第一个元素看做一个有序序列，把第二个元素到最后一个元素当成是未排序序列。
从头到尾依次扫描未排序序列，将扫描到的每个元素插入有序序列的适当位置。（如果待插入的元素与有序序列中的某个元素相等，则将待插入元素插入到相等元素的后面。）

import insertSort from '!!raw-loader!./answers/sort/insertSort.js';
import insertSortTest from '!!raw-loader!./answers/sort/insertSort.test.js';

<TestCode
   files={{
      "/insertSort.js": insertSort,
      "/insertSort.test.js": insertSortTest,
      "/fixture/testData.js": testData,
   }}
/>

</Answer>

## 希尔排序

1959年Shell发明，第一个突破O(n2)的排序算法，是简单插入排序的改进版。
它与插入排序的不同之处在于，它会优先比较距离较远的元素。希尔排序又叫缩小增量排序。

先将整个待排序的记录序列分割成为若干子序列分别进行直接插入排序，具体算法描述：
选择一个增量序列t1，t2，…，tk，其中ti>tj，tk=1；
按增量序列个数k，对序列进行k 趟排序；
每趟排序，根据对应的增量ti，将待排序列分割成若干长度为m 的子序列，分别对各子表进行直接插入排序。
仅增量因子为1 时，整个序列作为一个表来处理，表长度即为整个序列的长度。

<Answer>

import shellSort from '!!raw-loader!./answers/sort/shellSort.js';
import shellSortTest from '!!raw-loader!./answers/sort/shellSort.test.js';

<TestCode
   files={{
      "/shellSort.js": shellSort,
      "/shellSort.test.js": shellSortTest,
      "/fixture/testData.js": testData,
   }}
/>

</Answer>

## 归并排序

<Answer>

import mergeSort from '!!raw-loader!./answers/sort/mergeSort.js';
import mergeSortTest from '!!raw-loader!./answers/sort/mergeSort.test.js';

<TestCode
   files={{
      "/mergeSort.js": mergeSort,
      "/mergeSort.test.js": mergeSortTest,
      "/fixture/testData.js": testData,
   }}
/>
</Answer>

## 桶排序

<Answer>

import bucketSort from '!!raw-loader!./answers/sort/bucketSort.js';
import bucketSortTest from '!!raw-loader!./answers/sort/bucketSort.test.js';

<TestCode
   files={{
      "/bucketSort.js": bucketSort,
      "/bucketSort.test.js": bucketSortTest,
      "/fixture/testData.js": testData,
   }}
/>

</Answer>

## 计数排序

<Answer>

**算法步骤**

思想类似桶排序,但区间粒度更细,单一元素作为一个区间,适用于
区间范围狭小且固定,但数据量巨大的场景

1. 按照区间范围,将每个单一元素拆分为一个数组容器
2. 遍历数组,存入对应元素的数组
3. 合并所有离散数组即可

import countingSort from '!!raw-loader!./answers/sort/countingSort.js';
import countingSortTest from '!!raw-loader!./answers/sort/countingSort.test.js';

<TestCode
   files={{
      "/countingSort.js": countingSort,
      "/countingSort.test.js": countingSortTest,
      "/fixture/testData.js": testData,
   }}
/>

</Answer>
