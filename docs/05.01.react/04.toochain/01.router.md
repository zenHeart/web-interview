# 路由✅

## React Router 中 mode 有哪几种有什么区别？ {#p0-router}

<Answer>

Mode 就是使用 React Router 的方式，分为三种

* **Declarative** 基本使用，支持基础路由导航等逻辑
* **Data** 配置化使用，额外支持 loader、action 等能力
* **Framework** 框架化使用，支持不同渲染策略，如 SSR、SPA、RSC 等

<Tabs>

<TabItem value="DeclarativeMode">

import DeclarativeModeApp from '!!raw-loader!./answers/Router/Mode/DeclarativeMode.tsx';

<Sandpack
   customSetup={{
      dependencies: {
         'react-router': '^7',
      }
   }}
   options={{
      showConsole: true,
      editorHeight: '600px',
   }}
   template="react-ts"
   files={{
      '/App.tsx': DeclarativeModeApp,
   }}
/>

</TabItem>

<TabItem value="DataMode">

import DataModeApp from '!!raw-loader!./answers/Router/Mode/DataMode.tsx';

<Sandpack
   customSetup={{
      dependencies: {
         'react-router': '^7',
      }
   }}
   options={{
      showConsole: true,
      editorHeight: '600px',
   }}
   template="react-ts"
   files={{
      '/App.tsx': DataModeApp,
   }}
/>

</TabItem>

<TabItem value="FrameworkMode">

import FrameworkModeApp from '!!raw-loader!./answers/Router/Mode/FrameworkMode.tsx';

<Sandpack
   customSetup={{
      dependencies: {
         '@react-router/dev': '^7',
      }
   }}
   options={{
      showConsole: true,
      editorHeight: '600px',
   }}
   template="react-ts"
   files={{
      '/App.tsx': FrameworkModeApp,
   }}
/>

</TabItem>

</Tabs>

更详细的区别可以参考 [API 和 Mode 说明](https://reactrouter.com/start/modes#api--mode-availability-table)

</Answer>

## BrowserRouter、 HashRouter 、MemoryRouter 有什么区别？ {#p0-browser-hash-router}

<Answer>

|路由器|URL 表现|原理|典型场景|是否依赖服务器|
|---|---|---|---|---|
|BrowserRouter|普通路径 `/about`|HTML5 history API|SPA、生产环境|是|
|HashRouter|`#/about`|监听 location.hash|静态站点、无服务端|否|
|MemoryRouter|无 URL 变化|内存中维护 history|测试、非浏览器环境|否|

<Tabs>

<TabItem value="BrowserRouter">

import BrowserRouterApp from '!!raw-loader!./answers/Router/BrowserRouter.tsx';

<Sandpack
   customSetup={{
      dependencies: {
         'react-router': '^7',
      }
   }}
   options={{
      showConsole: true,
      editorHeight: '600px',
   }}
   template="react-ts"
   files={{
      '/App.tsx': BrowserRouterApp,
   }}
/>

</TabItem>

<TabItem value="HashRouter">

import HashRouterApp from '!!raw-loader!./answers/Router/HashRouter.tsx';

<Sandpack
   customSetup={{
      dependencies: {
         'react-router': '^7',
      }
   }}
   options={{
      showConsole: true,
      editorHeight: '600px',
   }}
   template="react-ts"
   files={{
      '/App.tsx': HashRouterApp,
   }}
/>

</TabItem>

<TabItem value="MemoryRouter">

import MemoryRouterApp from '!!raw-loader!./answers/Router/MemoryRouter.tsx';

<Sandpack
   customSetup={{
      dependencies: {
         'react-router': '^7',
      }
   }}
   options={{
      showConsole: true,
      editorHeight: '600px',
   }}
   template="react-ts"
   files={{
      '/App.tsx': MemoryRouterApp,
   }}
/>

</TabItem>

</Tabs>

:::tip

注意对比三个 Demo 输出路径区别可以直观反应不同模式对 url 的影响, 对于 BrowserRouter 需要服务端配置路由重写逻辑，以 nginx 为例

```nginx
location / {
   try_files $uri $uri/ /index.html;
}
```

:::

**延伸阅读**

* [BrowserRouter](https://reactrouter.com/en/main/routers/create-browser-router)
* [HashRouter](https://reactrouter.com/en/main/routers/create-hash-router)
* [MemoryRouter](https://reactrouter.com/en/main/routers/create-memory-router)

</Answer>

## React Router 的路径支持哪些匹配模式? {#p0-router-matching}

<Answer>

核心匹配逻辑如下

|匹配模式|描述|示例
|---|---|---|
|精确匹配|完全匹配路径|`/about` 只匹配 `/about`|
|动态片段|匹配动态路径|`/users/:id` 匹配 `/users/123`，123 可以通过 `const { id } = useParams()` 获取|
|可选片段|匹配可选路径|`/users/:id?` 匹配 `/users/123` 和 `/users/`|
|通配符|匹配任意路径|`/users/*` 匹配 `/users/123`、`/users/abc` 等, 注意通配符只能有一个且出现在最后一层， 可以通过 `const { '*': wildcard } = useParams()` 获取|
|索引路由|匹配父路径|`/users/*` 中的 `/users` 是索引路由，匹配 `/users`, 效果和嵌套路由中配置 `path=''` 等效，但是 index 路由更清晰|
|组合模式|支持多种匹配|`/users/:id/*` 匹配 `/users/123/details`，可以获取 `id` 和通配符部分|

import matchApp from '!!raw-loader!./answers/Router/Match.tsx';

<Sandpack
   customSetup={{
      dependencies: {
         'react-router': '^7',
      }
   }}
   options={{
      editorHeight: '800px',
   }}
   template="react-ts"
   files={{
      '/App.tsx': matchApp,
   }}
/>

:::tip

注意当存在多个匹配规则时，遵循如下规则

1. 越精确越优先匹配
2. 若优先级相同则按定义顺序匹配

React Router 内部实际上是通过一个权重计算来评估匹配的优先级，具体代码详见 [computeScore](https://github.com/remix-run/react-router/blob/eb19c3fae9e72aee3cfafb78bc87cae64c3b725b/packages/react-router/lib/router/utils.ts#L958) 各规则的分值如下，多条会进行累加计算。

```js
const dynamicSegmentValue = 3 // 动态片段
const indexRouteValue = 2 // 索引路由
const emptySegmentValue = 1 // 空片段
const staticSegmentValue = 10 // 静态路由例如 user
const splatPenalty = -2 // 通配符路由 *
```

:::

</Answer>

## 如何监听路由变化? {#p0-router-listen}

<Answer>

React Router 提供了 `useLocation` 钩子来获取当前路由信息，并可以通过 `useEffect` 钩子监听路由变化。

import useLocationApp from '!!raw-loader!./answers/Router/useLocation.tsx';

<Sandpack
   customSetup={{
      dependencies: {
         'react-router': '^7',
      }
   }}
   options={{
      editorHeight: '700px',
      showConsole: true,
   }}
   template="react-ts"
   files={{
      '/App.tsx': useLocationApp,
   }}
/>

**延伸阅读**

* [React Router useLocation](https://reactrouter.com/en/main/hooks/use-location)

</Answer>

## react-router 页面间如何传递信息？ {#p1-use-router}

<Answer>

|方式|传递入口|获取方式|典型场景|持久性|
|---|---|---|---|---|
|路由参数|`/users/:id`|`useParams()`|资源唯一标识|URL 可见，刷新保留|
|查询参数|`/users?id=123`|`useSearchParams()`|筛选、分页|URL 可见，刷新保留|
|路由状态|`navigate('/a', {state})`|`useLocation().state`|页面跳转携带临时数据|内存，刷新丢失|
|上下文|Context|useContext|全局/多层级共享|依赖组件树|

import useRouterApp from '!!raw-loader!./answers/Router/useRouter.tsx';

<Sandpack
   customSetup={{
      dependencies: {
         'react-router': '^7',
      }
   }}
   options={{
      editorHeight: '700px',
      showConsole: true,
   }}
   template="react-ts"
   files={{
      '/App.tsx': useRouterApp,
   }}
/>

:::tip
在实际开发中建议遵循 Restful 风格，优先考虑通过 URL 映射资源，避免使用内部状态保存一些筛选信息等，这样的好处是用户实际使用应用时，比如搜索或者查询时，URL 记录了用户的操作状态便于链接的分享和保存，避免由于存储在内存导致需要重新点选才能获取结果。
:::

**延伸阅读**

* [React Router useParams](https://reactrouter.com/en/main/hooks/use-params)
* [React Router useLocation](https://reactrouter.com/en/main/hooks/use-location)
* [React Router 跳转与参数传递](https://reactrouter.com/en/main/hooks/use-navigate)

</Answer>

## React Router 如何实现类似 Vue 导航守卫效果？{#p0-router-guide}

<Answer>

Vue 的导航守卫包含全局守卫、路由独享守卫和组件内守卫。
React Router 原生并不支持此能力，典型策略如下

1. **组件导航守卫** 通过 Hoc 包裹组件，在包裹组件中实现逻辑判断
2. **路由导航守卫** 通过 `loader` 和 `action` 实现路由级别的守卫逻辑
3. **全局导航守卫** 通过 `useEffect` 钩子监听路由变化，结合 `useLocation` 获取当前路由信息注册在全局页面实现

<Tabs>

<TabItem value="ComponentGuard">

import ComponentGuard from '!!raw-loader!./answers/Router/NavigationGuard/ComponentGuard.tsx';

<Sandpack
   customSetup={{
      dependencies: {
         'react-router': '^7',
      }
   }}
   options={{
      editorHeight: '600px',
      showConsole: true,
   }}
   template="react-ts"
   files={{
      '/App.tsx': ComponentGuard,
   }}
/>
</TabItem>

<TabItem value="RouteGuard">

import RouteGuard from '!!raw-loader!./answers/Router/NavigationGuard/RouterGuard.tsx';

<Sandpack
   customSetup={{
      dependencies: {
         'react-router': '^7',
      }
   }}
   options={{
      editorHeight: '600px',
      showConsole: true,
   }}
   template="react-ts"
   files={{
      '/App.tsx': RouteGuard,
   }}
/>

</TabItem>

<TabItem value="GlobalGuard">

import GlobalGuard from '!!raw-loader!./answers/Router/NavigationGuard/GlobalGuard.tsx';

<Sandpack
   customSetup={{
      dependencies: {
         'react-router': '^7',
      }
   }}
   options={{
      editorHeight: '600px',
      showConsole: true,
   }}
   template="react-ts"
   files={{
      '/App.tsx': GlobalGuard,
   }}
/>
</TabItem>

</Tabs>

</Answer>
