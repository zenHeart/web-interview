---
alwaysApply: true
description: 确保项目所有输出都使用中文
---

# 中文输出规则

## 概述

本规则确保项目中的所有输出内容都使用中文，包括但不限于：

- 代码注释
- 文档内容
- 用户界面文本
- 错误信息
- 日志输出
- 配置说明

## 适用范围

### 必须使用中文的场景

1. **文档内容** - 所有 Markdown 文档、README 文件
2. **代码注释** - JavaScript、TypeScript、CSS、HTML 等代码中的注释
3. **用户界面** - React 组件中的文本内容
4. **配置文件** - package.json、docusaurus.config.ts 等配置文件中的描述
5. **错误信息** - 自定义错误信息和提示
6. **日志输出** - 控制台输出的日志信息

### 代码示例

```javascript
// ✅ 正确：使用中文注释
function 计算总和(数组) {
  return 数组.reduce((总和, 当前值) => 总和 + 当前值, 0);
}

// ❌ 错误：使用英文注释
function calculateSum(array) {
  return array.reduce((sum, current) => sum + current, 0);
}
```

### 文档示例

```markdown
<!-- ✅ 正确：使用中文标题和内容 -->
# 前端面试题库

这是一个面向前端开发者的面试题库，包含 JavaScript、React、Vue 等核心技术。

<!-- ❌ 错误：使用英文标题和内容 -->
# Frontend Interview Questions

This is a frontend developer interview question bank covering JavaScript, React, Vue and other core technologies.
```

## 特殊处理

### 技术术语

- 优先使用中文技术术语
- 必要时可保留英文术语，但需提供中文解释
- 示例：`闭包（Closure）`、`原型链（Prototype Chain）`

### 变量和函数名

- 代码中的变量名和函数名可以使用英文（保持代码可读性）
- 但注释和文档说明必须使用中文
- 示例：

```javascript
// ✅ 正确
function debounce(func, delay) {
  // 防抖函数实现
  let timeoutId;
  return function(...args) {
    clearTimeout(timeoutId);
    timeoutId = setTimeout(() => func.apply(this, args), delay);
  };
}
```

### 外部链接和资源

- 外部链接的标题可以使用英文（如果原资源是英文）
- 但必须提供中文描述
- 示例：`[JavaScript MDN](https://developer.mozilla.org/en-US/docs/Web/JavaScript) JavaScript 官方文档`

## 质量检查清单

在提交代码前，请检查：

- [ ] 所有文档内容使用中文
- [ ] 代码注释使用中文
- [ ] 用户界面文本使用中文
- [ ] 错误信息使用中文
- [ ] 配置文件描述使用中文
- [ ] 技术术语有中文解释（如需要）

## 例外情况

以下情况可以保留英文：

1. **技术标准名称** - 如 HTTP、CSS、HTML 等
2. **框架和库名称** - 如 React、Vue、Webpack 等
3. **API 方法名** - 如 `getElementById`、`addEventListener` 等
4. **文件扩展名** - 如 `.js`、`.ts`、`.md` 等
5. **环境变量名** - 如 `NODE_ENV`、`PORT` 等

## 实施建议

1. **编辑器配置** - 设置编辑器默认语言为中文
2. **代码检查** - 使用 ESLint 等工具检查注释语言
3. **文档模板** - 创建中文文档模板
4. **团队培训** - 确保团队成员了解中文输出要求
